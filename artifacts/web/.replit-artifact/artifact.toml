kind = "web"
previewPath = "/next"
title = "PingAlert (Next.js)"
version = "1.0.0"
id = "artifacts/web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = ["/next"]
localPort = 22333

[services.development]
run = "pnpm --filter @workspace/web run dev"

[services.env]
PORT = "22333"
BASE_PATH = "/next"
NEXT_PUBLIC_BASE_PATH = "/next"
